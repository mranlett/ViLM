// ActorFilterCriteria.swift
// This type moved to LibraryCore so its matching and sorting logic is pure,
// unit-tested, and cannot drift from what the Actor Gallery displays. See
// `LibraryCore/Sources/LibraryCore/ActorFilterCriteria.swift` and
// `ActorFilterCriteria+Matching.swift`.
// Referenced here unqualified because every consumer already imports LibraryCore.
