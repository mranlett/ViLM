// AssetFilterCriteria.swift
// This type moved to LibraryCore so its matching logic is pure, unit-tested,
// and shared by every screen that filters videos (All Assets + Move Between
// Libraries). See `LibraryCore/Sources/LibraryCore/AssetFilterCriteria.swift`.
// Referenced here unqualified because every consumer already imports LibraryCore.
